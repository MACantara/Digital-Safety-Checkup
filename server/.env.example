# MongoDB connection string (required)
MONGODB_URI=mongodb://localhost:27017/digital-safety-checkup

# Port the API listens on (Railway injects this automatically)
PORT=3000

# Restrict CORS to your frontend Railway domain in production
# ALLOWED_ORIGIN=https://your-frontend.up.railway.app
