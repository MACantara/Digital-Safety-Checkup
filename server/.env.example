# MongoDB connection string (required).
# For Railway's MongoDB plugin, append ?authSource=admin — Railway authenticates
# against the admin database regardless of the database name in the URI.
MONGODB_URI=mongodb://<user>:<password>@<host>:<port>/<dbname>?authSource=admin

# Port the server listens on (Railway injects this automatically)
PORT=3000
