# URL of the API service — required at Vite build time.
# In Railway: add this to the frontend service's environment variables.
# Leave empty if running API and frontend on the same origin.
VITE_API_URL=http://localhost:3000
